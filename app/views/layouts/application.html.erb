<!DOCTYPE html>
<html>
  <head>
    <title>FirstRailsLecture</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div style="padding: 16px 8px; display: flex; justify-content: start; background: black; gap: 12px">
      <%= link_to "Home", root_path, style: "color: white; text-decoration: none;" %>
      <%= link_to "About", about_path, style: "color: white; text-decoration: none;" %>
      <%= link_to "Contacts", contact_path, style: "color: white; text-decoration: none;" %>
      
      <form action="/contact" method="get">
        <input type="text" name="member" placeholder="Who are you looking for?" value="<%= params["member"] %>">
        <input type="submit">
      </form>
    </div>

    <%= yield %>
  </body>
</html>
